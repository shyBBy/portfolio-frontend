import React, {createContext, useContext} from 'react'

export const MobileViewProvider = () => {

    interface MobileViewContextType {

    }

    const MobileViewContext = createContext(null)

    return(
        <>

        </>
    )
}